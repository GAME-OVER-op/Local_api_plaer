org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# libVLC 3.5.x тянет транзитивные AndroidX-зависимости, поэтому AndroidX обязателен.
# Наш код использует только классы фреймворка, поэтому jetifier не нужен.
android.useAndroidX=true
android.enableJetifier=false
org.gradle.caching=true
